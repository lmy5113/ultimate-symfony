{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
<h1>COnnexion</h1>
{% if error %}
<div class="alert alert-danger">
    {{ error.message }}
</div>
{% endif %}

{{ form_start(formView) }}
{{ form_widget(formView) }}
    <button type="submit" class="btn btn-success">Connextion</button>
{{ form_end(formView) }}
{% endblock %}
